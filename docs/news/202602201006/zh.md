*本文由英文自动翻译而来。*

我们很高兴地宣布新的网络控制台功能！

现在，您可以直接从浏览器访问虚拟机的终端，无需使用 SSH 客户端。这非常适合快速维护任务、调试或不在工作站时使用。

只需导航到虚拟机，然后单击控制台按钮，即可立即访问终端。

功能

- 通过 WebSocket 进行实时终端访问
- 连接状态指示器
- 断开时一键重新连接
- WebGL 加速渲染，性能流畅

当你需要排除可能妨碍 SSH 访问的网络故障时，这一功能尤其有用。

**注意：** 要使用网络控制台，需要先通过 SSH 设置用户密码，方法是运行 `passwd`。
